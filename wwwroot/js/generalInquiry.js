function inquiryBtn() {
    // TODO: 빈칸 예외처리 해야함
    /*
    var title = document.getElementById("title").value;
    var content = document.getElementById("content").value;

    var titleTD = document.getElementById("titleTD");
    var contentTD = document.getElementById("contentTD");

    if (title.length == 0 || content.length == 0) {
        var titleErr = document.createElement('p');
        titleErr.createTextNode("제목을 입력해주세요.");
        titleErr.style.color = "#ff0000";
        titleErr.style.fontSize = "15px";

        var contentErr = document.createElement('p');
        contentErr.createTextNode("내용을 입력해주세요.");
        contentErr.style.color = "#ff0000";
        contentErr.style.fontSize = "15px";

        if (title.length == 0) {
            titleTD.appendChild(titleErr);
        }

        if (content.length == 0) {
            contentTD.appendChild(contentErr);
        }

        return false;
    }
    */

    return true;
}